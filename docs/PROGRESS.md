# PROGRESS — 当前批次断点

> 📌 **"任务中途断了怎么续上"的单一事实来源。** 长任务/批次的实时状态放这里。
> 🔁 **新会话/断电重启后的开场动作**:① 读这份文件 → ② `git status` → ③ 从"进行中"那一步续上。
> 🧹 一个批次全部完成后 → **清空这份文件**(只留下面的空模板),把结果记进 `CHANGELOG.md`。
> 规则见 [docs/README.md §4](./README.md)。

---

## 当前批次
- **批次名**:A5 CLI/TUI,分支 `feature/issue-22-a5-cli-tui`(从 main `c8d0289` 拉出,在 A4b + 开源就绪度合并之后)。B0 `package.json`(`chalk`/`@inquirer/prompts` 依赖 + `bin` 字段)+ `src/cli/errors.ts`(带类型的 `UnsupportedProfileError`/`RunNotResumableError`)→ B1 `colors.ts`(chalk 主题,`escalationBanner()`,颜色等级强制设为 3 而不是靠 TTY 自动探测——这是我的判断,已在文件内写明)+ `diff-render.ts`(尽力而为的按行前缀 diff 上色器)→ B2 `gate-view.ts`(纯函数 `GatePayload -> string`,按 gate 类型区分,Escalation 用结构上明显不同的 banner 包起来)→ B3 `prompter.ts`(`Prompter` 接口、真实的 `InquirerPrompter`、可编排脚本的 `FakePrompter`)→ B4 `runner.ts` 新增 `getPendingInterrupt()`(这份 PRD 唯一动了 Loop 层的改动;**3 个变异测试抓出的回归**:漏掉的 run 校验、错误的 `thread_id`、写死的 `done: true`——全部抓到并改回)→ B5 `assemble.ts`(`assembleSubscriptionDeps()`——真实的依赖图接线;在 PRD §6.5 草图基础上给 `CliDeps` 扩了 `injector`/`memoryStore` 字段和一个可选的 `profilesRoot` override,两者都是必需的——已在文件内写明——分别服务于 `main.ts` 的 `start` 命令和 B8 的 fixture-profile 测试)→ B6 `run-loop.ts`(`runInteractiveLoop()`——共享的启动/续跑编排器;**4 个变异测试抓出的回归**:永远批准的 G1/G3、永远放弃的 Escalation、G1/G3 渲染分发写反——最后一个一开始溜过了已有测试套件,是在把 happy-path 测试加强到去断言打印输出里真实的 `[G1]`/`[G3]` 标题标签之后才抓到的,随后重新验证了 red→green)→ B7 `main.ts`(经 `node:util.parseArgs` 解析 argv,`start`/`resume`/`list` 分发,每个错误都被捕获并打印成 `Name: message`——绝不打印原始 stack trace——SIGINT handler 每次调用都装上/卸掉,确保重复调用不会泄漏 listener)+ `bin.ts`(真实的两行生产入口;手动做了烟测,跑法是 `pnpm build && node dist/cli/bin.js <cmd>`——argv 错误路径干净打印且退出码为 1,`aeloop list` 对着真实的 subscription profile 跑出 "No resumable runs." 且退出码为 0)→ B8 硬核纵切验收(`src/cli.e2e.test.ts`——真实的 `main()` 分发 + 真实的 subscription-profile 依赖图(通过 `assembleSubscriptionDeps()` 的 `profilesRoot` override,一个临时目录里放着真实 `config.yaml`/persona 的忠实拷贝,外加 fixture 版 `bin` override)+ 真实的 cli-bridge fixture 子进程 + 可编排脚本的 `FakePrompter`,happy path 和阈值升级路径都跑了,两条路径落盘后都是 `applied: true`)→ B9 回写文档(本文件/ROADMAP/CHANGELOG/README + ai-agent 仓库的 `CHARTS/knowledge/aeloop.md`)。**368/368 测试全绿**,`pnpm build`(tsc strict + `noUncheckedIndexedAccess`)/`pnpm lint`(`tsc --noEmit`)均干净。对 main 跑 `git diff --stat`:除了 `src/cli/**` 之外没有其他改动,唯一例外是 `runner.ts` 那一处纯新增的导出(见 §5)以及对应的 `runner.test.ts` 新增用例,再加上 `package.json`/`pnpm-lock.yaml`(两个新依赖)——和 PRD §10 的验收标准分毫不差。**尚未 commit/push(等指挥官批准)——独立复审中。** 详见 `docs/feature/a5-cli-tui/PRD.md`。
- 上一批次(A4b Loop 编排收尾)结果:B0-B7 全部完成,276/276 测试全绿,详见 `docs/feature/a4b-loop/PRD.md` + CHANGELOG.md。
