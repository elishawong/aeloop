# PROGRESS — Current Batch Resume Point

> 📌 **Single source of truth for "resume mid-task after a shutdown."** Live status for long tasks/batches lives here.
> 🔁 **New session/restart-after-shutdown opening**: ① read this file → ② `git status` → ③ pick up from the "in progress" step.
> 🧹 Once a batch is fully complete → **clear this file** (leave only the empty template below), record the outcome in `CHANGELOG.md`.
> Rules in [docs/README.md §4](./README.md).

---

## Current Batch
- **Batch name**: A5 CLI/TUI, branch `feature/issue-22-a5-cli-tui` (branched from main `c8d0289`, after the A4b + open-source-readiness merges). B0 `package.json` (`chalk`/`@inquirer/prompts` deps + `bin` field) + `src/cli/errors.ts` (typed `UnsupportedProfileError`/`RunNotResumableError`) → B1 `colors.ts` (chalk theming, `escalationBanner()`, color level forced to 3 rather than TTY-autodetected — my judgment call, documented in-file) + `diff-render.ts` (best-effort line-prefix diff colorizer) → B2 `gate-view.ts` (pure `GatePayload -> string` per gate type, Escalation wrapped in the structurally-distinct banner) → B3 `prompter.ts` (`Prompter` interface, real `InquirerPrompter`, scripted `FakePrompter`) → B4 `runner.ts` gains `getPendingInterrupt()` (the one Loop-layer change this PRD makes; **3 mutation-tested regressions**: missing-run guard, wrong `thread_id`, hardcoded `done: true` — all caught, reverted) → B5 `assemble.ts` (`assembleSubscriptionDeps()` — real dependency-graph wiring; extended the PRD §6.5 sketch with `injector`/`memoryStore` fields on `CliDeps` and an optional `profilesRoot` override, both necessary — documented in-file — for `main.ts`'s `start` command and B8's fixture-profile testing respectively) → B6 `run-loop.ts` (`runInteractiveLoop()` — shared start/resume orchestrator; **4 mutation-tested regressions**: always-approved G1/G3, always-abandon Escalation, swapped G1/G3 render dispatch — the last one initially slipped past the existing test suite, caught only after strengthening the happy-path test to assert on the real `[G1]`/`[G3]` heading tags in printed output, then re-verified red→green) → B7 `main.ts` (argv parsing via `node:util.parseArgs`, `start`/`resume`/`list` dispatch, every error caught and printed `Name: message` — never a raw stack trace — SIGINT handler installed/removed per call so repeated calls never leak listeners) + `bin.ts` (real two-line production entry point; manually smoke-tested via `pnpm build && node dist/cli/bin.js <cmd>` — argv-error paths print cleanly with exit code 1, `aeloop list` against the real subscription profile prints "No resumable runs." with exit code 0) → B8 hard vertical slice (`src/cli.e2e.test.ts` — real `main()` dispatch + real subscription-profile dependency graph (via `assembleSubscriptionDeps()`'s `profilesRoot` override, a temp dir with a faithful copy of the real `config.yaml`/personas plus fixture `bin` overrides) + real cli-bridge fixture subprocesses + scripted `FakePrompter`, both the happy path and the threshold-escalation path, both ending `applied: true` on disk) → B9 wrote back docs (this file/ROADMAP/CHANGELOG/README + ai-agent repo `CHARTS/knowledge/aeloop.md`). **368/368 tests green**, `pnpm build` (tsc strict + `noUncheckedIndexedAccess`) / `pnpm lint` (`tsc --noEmit`) both clean. `git diff --stat` against main: no changes outside `src/cli/**` except the single, additive `runner.ts` export (§5) and its `runner.test.ts` additions, plus `package.json`/`pnpm-lock.yaml` (the two new deps) — matching PRD §10's acceptance criterion exactly. **Not yet committed/pushed (awaiting commander approval) — pending independent review.** See `docs/feature/a5-cli-tui/PRD.md` for details.
- Previous batch (A4b Loop orchestration wrap-up) outcome: B0-B7 all complete, 276/276 tests green, see `docs/feature/a4b-loop/PRD.md` + CHANGELOG.md for details.
