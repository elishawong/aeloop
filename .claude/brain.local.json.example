{
  "_comment": "复制这份文件成 .claude/brain.local.json（已 gitignore，不会被提交）。issue #88 B9：解决 IDE/图形界面启动的 Claude Code 会话不继承 shell profile export 的坑——AELOOP_BRAIN_IDENTITY_DB 环境变量优先，读不到时 hook 会 fallback 读这份本地配置的 identityDbPath 字段。见 docs/conductor-brain-layer/WAKE-GREETING-RUNBOOK.md 的『IDE 启动读不到 env 的坑』一节。",
  "identityDbPath": "/absolute/path/to/your/identity.db",
  "_comment_taskSource": "非必需字段，缺省 \"none\"（issue #103：shipped 默认零 GitHub，「现在在途」/「Idea Queue」不渲染，seed 不调 gh）。设成 \"github\" 可 opt-in 接回 GitHub issue 同步 + 看板渲染（AELOOP_BRAIN_TASK_SOURCE 环境变量优先级更高，两者都设时以 env 为准）。全局模式（AELOOP_BRAIN_GLOBAL_MODE=1）下这个字段不生效，见 .claude/hooks/lib/task-source.mjs。",
  "taskSource": "none"
}
